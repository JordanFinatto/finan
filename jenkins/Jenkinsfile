pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                sh "./var/lib/jenkins/workspace/finan/vendor/bin/sail down"
                sh "git pull"
                sh "./var/lib/jenkins/workspace/finan/vendor/bin/sail up -d"
            }
        }
    }
}
