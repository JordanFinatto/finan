pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                cd /home/univates/finan
                ./vendor/bin/sail down
                git pull
                ./vendor/bin/sail up -d
            }
        }
    }
}
